package devdf.plugins.docman

import androidx.core.content.FileProvider

/** This is a FileProvider class for the plugin
 * This class is used to provide the plugin with the necessary permissions to access the file system.
 */
class DocManFileProvider : FileProvider()